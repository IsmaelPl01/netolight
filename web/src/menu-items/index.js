import statistics from './statistics';
import control from './control';
import infrastructure from './infrastructure';

const menuItems = {
  items: [statistics, control, infrastructure]
};

export default menuItems;
